PKG_NAME="xfsprogs"
PKG_VERSION="2ab8ecb"
PKG_GIT_URL="git://oss.sgi.com/xfs/cmds/xfsprogs.git"
PKG_DEPENDS_HOST="toolchain"
PKG_DEPENDS_TARGET="toolchain util-linux zlib lzo linux"

PKG_SECTION="devel"
PKG_IS_ADDON="no"
PKG_AUTORECONF="no"

pre_configure_target() {
  CFLAGS="$CFLAGS -fPIC -DPIC"
  strip_lto
}

pre_configure_target() {
  make CC=$CC LOCAL_CONFIGURE_OPTIONS="--host=$TARGET_NAME --build=$HOST_NAME --disable-shared"
  ./release.sh
}
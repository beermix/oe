diff --git a/build/toolchain/linux/BUILD.gn b/build/toolchain/linux/BUILD.gn
index 4e9ff44..c928e0d 100644
--- a/build/toolchain/linux/BUILD.gn
+++ b/build/toolchain/linux/BUILD.gn
@@ -145,15 +145,20 @@ clang_toolchain("clang_x64_v8_mips64") {
 }
 
 gcc_toolchain("x64") {
-  cc = "clang"
-  cxx = "clang++"
+  toolchainprefix = "x86_64-libreelec-linux-gnu-"
+  cc = "${toolchainprefix}gcc"
+  cxx = "${toolchainprefix}g++"
 
-  readelf = "readelf"
-  nm = "nm"
-  ar = "ar"
+  readelf = "${toolchainprefix}readelf"
+  nm = "${toolchainprefix}nm"
+  ar = "${toolchainprefix}ar"
   ld = cxx
 
-  # Output linker map files for binary size analysis.
+  extra_cflags = "-mfpmath=sse -fdiagnostics-color=always -fno-unwind-tables -fno-asynchronous-unwind-tables"
+  extra_cxxflags = "-mfpmath=sse -Wno-attributes -Wno-comment -Wno-unused-variable -Wno-noexcept-type -Wno-register -Wno-strict-overflow -Wno-deprecated-declarations -fdiagnostics-color=always -fno-unwind-tables -fno-asynchronous-unwind-tables"
+  extra_cppflags = "-DNO_UNWIND_TABLES"
+
+
   enable_linker_map = true
 
   toolchain_args = {
@@ -163,6 +168,26 @@ gcc_toolchain("x64") {
   }
 }
 
+gcc_toolchain("x64_host") {
+  toolchainprefix = "host-"
+  cc = "${toolchainprefix}gcc"
+  cxx = "${toolchainprefix}g++"
+
+  readelf = "readelf"
+  nm = "nm"
+  ar = "ar"
+  ld = cxx
+
+ # Output linker map files for binary size analysis.
+ enable_linker_map = true
+
+ toolchain_args = {
+   current_cpu = "x64"
+   current_os = "linux"
+   is_clang = yes
+  }
+}
+
 clang_toolchain("clang_mipsel") {
   toolchain_args = {
     current_cpu = "mipsel"
